<h1>My Tasks</h1>

<% @tasks.each do |task| %>
<p><%= link_to task.title,task_path(task) %><br><%= link_to "Delete a task" ,  destroy_path(task), data: { turbo_method: 'delete', turbo_confirm: 'Are you sure?' }  %></p>
<% end %>

<%= link_to "Add a task", tasks_new_path %>
